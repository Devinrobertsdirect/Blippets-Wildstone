# Original artwork (not shipped)

Keep full-resolution source PNGs here (e.g. 1024×1024). These are **not** bundled
into the game — they're the master copies.

The runtime-ready, downscaled sprites go in `public/blippets/` using the
filename convention documented there.
